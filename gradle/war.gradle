apply plugin: "war"

bootWar {
    mainClassName = "org.muellners.finscale.core.CoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
